 #!/bin/sh
set -x

# /usr/bin/mc config host add dsb-minio http://${MINIO_HOST_URI }:${ MINIO_HOST_PORT } ${MINIO_ACCESS_KEY} ${MINIO_SECRET_KEY};
# /usr/bin/mc rm -r --force dsb-minio/builds;
# /usr/bin/mc mb dsb-minio/builds;
# /usr/bin/mc policy download dsb-minio/builds;

# exit 0;
